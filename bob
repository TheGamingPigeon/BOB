term.clear()
print("left right or back")
type = io.read()
	local i = 1
	repeat
 
		if turtle.compare()==true then
			redstone.setOutput(type, true)
		end
		if turtle.compare()==false then
			redstone.setOutput(type, false)
		end
	until i==5
